var test=require('./test')

////处理ori变化的数据 sync into dest//////处理ori变化的数据 sync into dest//////处理ori变化的数据 sync into dest//////处理ori变化的数据 sync into dest
test.updateDataIntoDest().then(res => {
    console.log(res)
})